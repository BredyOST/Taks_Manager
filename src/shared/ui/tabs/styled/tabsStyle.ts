import styled from "styled-components";
import {CoverTabs} from "../../../../pages/home/styled/homeStyled";
import {TabContext, TabList} from "@mui/lab";
import {Box} from "@mui/material";


export const TabsWrapper = styled(CoverTabs)`
  
`;

export const TabsContext  = styled(TabContext)`
  
`;

export const TabsBox = styled(Box)`
  
`;

export const TabsItemCover = styled(TabList)`
    .MuiButtonBase-root {
        
    }
`;