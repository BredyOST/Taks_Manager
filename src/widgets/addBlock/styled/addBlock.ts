import styled from 'styled-components';
import { rem } from '../../../app/styles/common';

export const WrapperAddBlock = styled.div`
    display: flex;
    flex-direction: column;
    row-gap: ${rem(10)};
`;

export const CoverInputAddBlock = styled.div`
    flex: 1 1 auto;
    display: flex;
    min-width: 100%;
`;
